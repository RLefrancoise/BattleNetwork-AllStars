#ifndef SCREENS_HPP
#define SCREENS_HPP

#include "Screen.hpp"

enum {
	SCREEN_EXIT = -1,
	SCREEN_TITLE,
	SCREEN_BATTLEMAP,
	SCREEN_FIELDMAP,
	SCREEN_CHARACTER_SELECT,
	SCREEN_LOADING
};

#endif
